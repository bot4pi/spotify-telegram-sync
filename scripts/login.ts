import { startClient, tg } from '../src/client.js'

/**
 * Разовый вход в Telegram: телефон, код, пароль 2FA.
 *
 * Создаёт файл сессии, указанный в `TG_SESSION_PATH`. Дальше программа берёт
 * его сама и ничего не спрашивает. Файл сессии — это полный доступ к аккаунту,
 * он остаётся на твоей машине и никуда не отправляется.
 */
await startClient()
await tg.destroy()
